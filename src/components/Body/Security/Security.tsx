import React from 'react'

interface SecurityProps {

}

export const Security: React.FC<SecurityProps> = ({}) => {
        return (
            <span className='font-bold text-[32px]'>Security</span >
        );
}